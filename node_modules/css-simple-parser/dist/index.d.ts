import type { AST, ROOT_NODE, NODE } from './types.js';
declare const Parser: {
    parse: (css: string) => AST;
    stringify: (ast: AST) => string;
    traverse: (ast: AST | NODE, fn: (node: NODE) => void) => void;
};
export default Parser;
export type { AST, ROOT_NODE, NODE };
